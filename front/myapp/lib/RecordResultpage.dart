import 'package:flutter/material.dart';

class RecordResultpage extends StatelessWidget {
  const RecordResultpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text('안녕')));
  }
}
